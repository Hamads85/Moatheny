# تحليل مشكلة اتجاه القبلة - الرياض

## 📍 الإحداثيات المستخدمة
- **الرياض**: 24.8453°N, 46.6753°E
- **الكعبة**: 21.4224779°N, 39.8251832°E

## ✅ الخوارزمية صحيحة

### الحساب اليدوي
```
deltaLon = 39.8251832° - 46.6753° = -6.8501°

y = sin(-6.8501°) × cos(21.4224779°) = -0.1110
x = cos(24.8453°) × sin(21.4224779°) - sin(24.8453°) × cos(21.4224779°) × cos(-6.8501°)
x = -0.0569

bearing = atan2(-0.1110, -0.0569) = -116.86°
bearing_normalized = -116.86° + 360° = 243.14°
```

**النتيجة الصحيحة**: ~243° (جنوب غرب) ✓

## ❌ المشكلة المكتشفة

### الأعراض
- **الاتجاه المتوقع**: ~243° (جنوب غرب)
- **الاتجاه المعروض**: 154° SE (جنوب شرق) ✗

### التحليل
عندما يتم تمرير الإحداثيات بشكل **معكوس** (latitude و longitude مبدلان):
- الرياض: 46.6753°N, 24.8453°E (خطأ!)
- النتيجة: ~149° (قريب من 154° المعروض)

## 🔍 الأسباب المحتملة

### 1. إحداثيات خاطئة في قاعدة البيانات
- المدينة المحفوظة قد تحتوي على إحداثيات معكوسة
- التحقق: فحص `SavedCity` في `UserDefaults`

### 2. خطأ في تمرير المعاملات
- **الكود الحالي صحيح**:
  ```swift
  qiblaDirection = QiblaCalculator.calculateQiblaDirection(
      from: loc.coordinate.latitude,    // ✓ صحيح
      longitude: loc.coordinate.longitude // ✓ صحيح
  )
  ```

### 3. خطأ في GPS
- إذا كان المستخدم يستخدم GPS، قد تكون الإحداثيات من `LocationService` خاطئة

## 🛠️ الحلول المقترحة

### الحل 1: التحقق من الإحداثيات المحفوظة
```swift
// في updateQibla()، أضف logging:
print("📍 الموقع: \(loc.coordinate.latitude), \(loc.coordinate.longitude)")
print("🕋 اتجاه القبلة: \(qiblaDirection)°")
```

### الحل 2: إضافة validation للإحداثيات
```swift
// التحقق من أن latitude في النطاق الصحيح (-90 إلى 90)
guard abs(coord.latitude) <= 90 else {
    print("⚠️ خطأ: latitude خارج النطاق: \(coord.latitude)")
    return
}
```

### الحل 3: إصلاح الإحداثيات المعكوسة تلقائياً
```swift
// إذا كانت الإحداثيات تبدو معكوسة (latitude > 90 أو longitude > 180)
// قم بتبديلها تلقائياً
```

## 📊 اختبارات التحقق

### اختبار 1: الرياض الصحيحة
```
Input: 24.8453°N, 46.6753°E
Expected: ~243° SW
Actual: 242.86° ✓
```

### اختبار 2: الرياض معكوسة
```
Input: 46.6753°N, 24.8453°E (خطأ!)
Expected: N/A
Actual: 149.20° ✗ (قريب من 154° المعروض!)
```

## ✅ الخلاصة

1. **الخوارزمية صحيحة 100%** - الحساب اليدوي يؤكد ذلك
2. **المشكلة في الإحداثيات** - إما محفوظة بشكل خاطئ أو معكوسة
3. **الحل**: التحقق من الإحداثيات في `cityStore.activeCoordinate` وإصلاحها

## 🔧 الخطوات التالية

1. إضافة logging مفصل في `updateQibla()`
2. التحقق من الإحداثيات المحفوظة في `UserDefaults`
3. إضافة validation للإحداثيات قبل الحساب
4. إصلاح أي إحداثيات معكوسة تلقائياً
